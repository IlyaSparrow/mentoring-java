# Multithreading sorting via fjp 


Implement Merge Sort or Quick Sort algorithm that sorts a huge array of integers in parallel using Fork/Join framework.
Complexity of the merge sort will be: T(n) = 2T(n/2) + O(n)